# Copy to .env and adjust. .env is git-ignored and must never be committed.

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# The development database. Created by `pnpm db:reset`.
DATABASE_URL=postgresql://postgres:atarimae_dev@localhost:5432/atarimae_dev

# The database used by integration tests and Playwright. Wiped freely.
TEST_DATABASE_URL=postgresql://postgres:atarimae_dev@localhost:5432/atarimae_test

# Connects to the maintenance database to CREATE/DROP the two above.
# Only used by scripts/db-reset.mjs, never by the running server.
ADMIN_DATABASE_URL=postgresql://postgres:atarimae_dev@localhost:5432/postgres

# ---------------------------------------------------------------------------
# Secrets
# ---------------------------------------------------------------------------
# Master key for the secret store (SMTP passwords, call provider secrets).
# 32 bytes, base64-encoded. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# WARNING: losing this key makes every stored external credential permanently
# unrecoverable. Back it up together with, but NOT inside, the database dump.
ENCRYPTION_KEY_CURRENT=key01:REPLACE_ME_WITH_32_BYTES_BASE64

# Set during key rotation so ciphertext written with the old key still decrypts.
# Leave empty when not rotating.
ENCRYPTION_KEY_PREVIOUS=

# Signs session cookies. 32+ random bytes, base64.
SESSION_SECRET=REPLACE_ME_WITH_32_BYTES_BASE64

# ---------------------------------------------------------------------------
# Docker deployment only
# ---------------------------------------------------------------------------
# docker-compose.yml reads these. They are ignored when running locally.
#
# PUBLIC_ORIGIN below must be the address people actually type into a browser:
# it is used for CORS and for the links inside notification emails, so getting
# it wrong produces mail nobody can act on.
POSTGRES_PASSWORD=REPLACE_ME_WITH_A_STRONG_PASSWORD
POSTGRES_USER=atarimae
POSTGRES_DB=atarimae
APP_PORT=3000

# ---------------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
HOST=127.0.0.1

# Public origin of the web client, used for CORS and links in emails.
PUBLIC_ORIGIN=http://localhost:5173

# Addresses whose X-Forwarded-For may be believed — your reverse proxy's own,
# as a comma-separated list of IPs or CIDR ranges. Leave it unset if nothing
# sits in front of Atarimae.
#
# The header is set by whoever sends the request. Believing it from anybody
# hands every client the ability to choose its own address, and the address is
# what the sign-in rate limit is keyed on and what audit_logs records — ten
# attempts per address becomes unlimited attempts. Leaving this unset behind a
# proxy is the safe mistake, not the dangerous one: the whole office shares one
# rate-limit budget, which is visible and annoying rather than silent.
#
# TRUSTED_PROXY_IPS=172.18.0.0/16

# Where chat attachments are written. Relative paths are resolved from the
# working directory; `var/` is git-ignored.
#
# In Docker this is a mounted volume — see docker-compose.yml. A deployment
# that leaves it inside the container loses every uploaded file on the next
# rebuild, while the database keeps the rows that point at them.
ATTACHMENT_ROOT=./var/attachments

# trace | debug | info | warn | error
LOG_LEVEL=debug
